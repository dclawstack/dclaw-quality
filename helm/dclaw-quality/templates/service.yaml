apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-quality.fullname" . }}
  labels:
    {{- include "dclaw-quality.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-quality.selectorLabels" . | nindent 4 }}
