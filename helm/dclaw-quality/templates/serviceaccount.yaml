apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dclaw-quality.serviceAccountName" . }}
  labels:
    {{- include "dclaw-quality.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
